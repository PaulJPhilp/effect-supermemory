/**
 * Constants for memory client validation and limits.
 */

/**
 * Maximum length for a memory key in characters.
 */
export const MAX_MEMORY_KEY_LENGTH = 255;

/**
 * Maximum size for a memory value in bytes (1MB).
 */
export const MAX_MEMORY_VALUE_SIZE_BYTES = 1024 * 1024;

/**
 * Maximum length for a namespace in characters.
 */
export const MAX_NAMESPACE_LENGTH = 64;
